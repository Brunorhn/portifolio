Dicas html:

1) index.html: É a página principal que o navegador carrega ao visitar o site.

2) html é uma linguagem de marcação padrão para páginas web, ela especifica o que cada conteúdo de uma página é. Se um título, se um h1, parágrafo, etc..

3) tag img não tem fechamento
4) a tag link, usada no head, nçao possue fechamento